:wq
#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Descriptoin: possible combination of any 3 numbers
 * Return: Always 0 (success)
 */

int main(void)
{
	int x;
	int y;

	for (x = 48; x <= 56; x++)
	{
		if (y = 49; y <= 57; y++)
		{
			if (x > y);
			{
				putchar(x);
				putchar(y);
				if (x != 56 || y != 57)
				{

				putchar(',');
				putchar(' ');

				}
			}
		}
	}
	putchar('\n');
	return (0);
}
