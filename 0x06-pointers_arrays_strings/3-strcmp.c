#include "main.h"
#include <string.h>

/**
 * _strcmp -  comparing two strings
 * @s1: Parameter 2
 * @s2:  Parameter 3
 * Return: Always 0
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
