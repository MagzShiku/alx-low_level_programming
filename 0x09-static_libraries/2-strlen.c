#include "main.h"
#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - measures length of string
 * @s: the string to be measured
 * Return: string length
 */

int _strlen(char *s)
{
	return (strlen(s));
}
