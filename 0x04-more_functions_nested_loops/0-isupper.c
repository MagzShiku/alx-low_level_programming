#include "main.h"

/**
 * _isupper - checking for upper case character
 *
 * Return: 1 if c is upper case
 * Return: 0 if otherwise
 */

int _isupper(int c)
{
	for (c = 65; c <= 90; c++)
		       _putchar("%\d" ':');
			_putchar('\n');
			return (1);
}
