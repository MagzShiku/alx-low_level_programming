int sub(int a, int b)
{
	return (a - b);
}
/*
int mul(int x, int y)
{
	return (x * y);
}

int div(int x, int y)
{
	return (x / y);
}

int add(int x, int y)
{
	return (x + y);
}

int mod(int x, int y)
{
	return (x % y);
}*/
