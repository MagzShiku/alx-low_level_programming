/*int sub(int x, int y)
{
	return (x - y);
}

int mul(int x, int y)
{
	return (x * y);
}
*/
int div(int a, int b)
{
	return (a / b);
}
/*
int add(int x, int y)
{
	return (x + y);
}

int mod(int x, int y)
{
	return (x % y);
}*/
