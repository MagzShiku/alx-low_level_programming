/*int sub(int x, int y)
{
	return (x - y);
}
*/
int mul(int a, int b)
{
	return (a * b);
}
/*
int div(int x, int y)
{
	return (x / y);
}

int add(int x, int y)
{
	return (x + y);
}

int mod(int x, int y)
{
	return (x % y);
}*/
