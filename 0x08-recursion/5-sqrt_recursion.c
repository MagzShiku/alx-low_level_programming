#include "main.h"
#include <stdio.h>
#include <math.h>

i
/**
 * _sqrt_recursion -  a function that returns the natural square root of a number.
 * @n: number whose square root i]has been printed
 * Return: -1 if  n does not have a square root
 */

int _sqrt_recursion(int n)
{
	int _sqrt_recursion = 
	return (sqrt(n, 1));

}
